<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/relativeLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.example.macbookair.demo.MainActivity">

    <TextView
        android:id="@+id/txt"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Contacts"
        android:textAlignment="center"
        android:textColor="@color/txtColor"
        android:textSize="40sp" />

    <ListView
        android:id="@+id/listView"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentStart="true"
        android:layout_below="@id/txt"
        android:layout_marginTop="130dp"
        android:listSelector="@drawable/my_selector" />

    <EditText
        android:id="@+id/addedContactName"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/txt"
        android:layout_centerInParent="true"
        android:hint="@string/name"
        android:textSize="15sp" />

    <EditText
        android:id="@+id/addedContactPhone"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@id/addedContactName"
        android:layout_centerInParent="true"
        android:hint="@string/phone_number"
        android:inputType="phone"
        android:textSize="15sp" />

    <Button
        android:id="@+id/btnSave"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_below="@id/addedContactPhone"
        android:layout_centerInParent="true"
        android:onClick="save"
        android:text="@string/save"
        android:textSize="20sp" />


</RelativeLayout>
